# 简洁

本项目是在供应商的示例工程中做的一个变种

实现一个简单的变声器 + 录音器，通过 ADC 按键上拉模式连接则是变声器，下拉模式连接则是录音器

变声器：高音调、低音调、机器人音调、停止 四种模式按键循环切换

录音器：短按播放录音、长按开始录音（可以在播放录音时再次短按或长按）

开发语言：C、汇编

开发工具：G+LINK Pro V2编程仿真器、unSPIDE

第三方库：由芯片供应商提供

开发时间：一个月

心得体会：
引脚 CMPADC 同一时间有 1个输入源，那么要么由麦克风使用，要么由按键 ADC 使用
按键 ADC 的数字 IO 和 ADC 功能也是互斥的，数字 IO 读取时会干扰 ADC 的值，所以这两个功能是不能同时使用的

涉及问题：
按键状态机切换、按键锁存消抖、麦克风前端增益、自动录音触发和释放阈值调节、flash 数据读写问题、flash 芯片校验问题和代码优化问题

后续内容是个人杂项记录

## 测试录音回放

```
1. 上电
2. 按 IOA0：开始录音
3. 对麦克风说话 2~5 秒
4. 按 IOA1：停止录音
5. 按 IOA2：播放录音并进入/切换 VC4 音效
```

每次按 IOA2，`VC_Mode` 会加 1，所以效果会在多种模式之间切换。

## 测试预置资源

```
1. 按 IOA3：播放预置 A1800 资源
2. 再按 IOA3：切换下一条资源
3. 按 IOA4：停止
```

## 测试声音检测

```
1. 按 IOA5：开启 MIC EnvDet
2. 对麦克风说话
3. 看板子上是否有 LED 或 IO 状态变化
4. 按 IOA6：关闭检测
```

F_SACM_DVR1800_SendDAC1
录音过程中，喇叭监听麦克风的声音

F_USER_DVR1800_WriteData
把录好的声音写到 flash

F_DVR1800_SwitchINTtoRAM
结束录音？查看0x0f0000有无数据

#### 开发日志

测试变声器最长录音时间，8M，1IO，\_test 版本程序，按键切换，至少能录1分钟

\_test
第一次 stop 之后必须走一遍 loop 确保写入录音长度
第二次 stop 才是根据录音长度去播放录音
录音录完了再次调用 stop 并走一遍 loop 和清看门狗
sys_mode
0 开机初始值
1 开启录音前调用停止录音 stop（停止时DVR停止EnvDet停止麦克风flag停止）
2 播放录音前再次调用 stop 并清看门狗（停止时A1800停止VC4停止麦克风flag停止
停止调用了 stop sys_mode 都是回到 0

5月底最新版代码，加了 8M flash 后数据异常
能上拉模式，自动监听式录音，但是播放不了滴声
用 \_test 代码能录音后播放，对比一下 beyond compare

现在用 bcmp 把 8M 和 \_test 的逻辑合并到最新版，bug 无法解决

以 \_test 为基础，重新制作变声器

首先确保 \_test 可以 录音/停止/回放/滴声
同样的代码，在自研板上播放滴声很短促

拆分 5月底最新版代码，逐步将功能移植到 \_test

先不急着移植，把刚才的发现修改到 5月底最新版试一试效果，不行还是读取异常

还是干回将功能移植到 \_test 代码吧

① 自动监听 / 静音检测 / 监听模式，有声音则自动录音，无声音则自动停止，然后播放，播放完毕回到监听模式
② 上拉按键，滴声，切换 4 种模式的状态机
注意前端放大增益 触发attack的阈值 低音音色和 静音释放 时间
③ ADC 按键
④ 短按固定录音10秒，长按播放
功能整合

变声器 v1.3 改动说明
变声数据和录音数据分开存储，互不干扰

降低变声触发的阈值，低声说话也能录到
低沉变声有 12个变调档位，现在选择 -3
实时监听变声回放，限制单次变声最长 10秒

录音器长按 2 秒自动录音，无需松手
无有效录音数据时，不播放
录音结束后，播两次滴提示音